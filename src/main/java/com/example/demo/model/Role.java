package com.example.demo.model;

public enum Role {
    // If the test expects the full qualified name:
    ROLE_USER,
    ROLE_ADMIN, 
    ROLE_SUPPLIER
    
    // If you currently have just USER, ADMIN, SUPPLIER, change them above.
}
