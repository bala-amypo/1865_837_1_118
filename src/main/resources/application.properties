spring.application.name=demo
# Standard port
server.port=9001
server.forward-headers-strategy=framework

# Database Configuration
# I named the DB 'supply_chain_db', it will auto-create if missing
spring.datasource.url=jdbc:mysql://localhost:3306/supply_chain_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# === REQUIRED JWT PROPERTIES (This fixes your error) ===
app.jwtSecret=VerySecretKeyForSupplyChainAnalyzerApplication123456
app.jwtExpirationInMs=3600000